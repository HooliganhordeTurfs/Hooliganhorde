query Gamedays {
  gamedays(first: 24, orderBy: gameday, orderDirection: desc) {
    id
    gameday
    price
    deltaHooligans
  }
  fields(first: 24, orderBy: gameday, orderDirection: desc) {
    id
    gameday
    rage
    intensity
  }
}